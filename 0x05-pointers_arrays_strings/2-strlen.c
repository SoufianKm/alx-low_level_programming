#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - calculate the length of a string
 * @s : pointer string parameter
 * Return: length of a string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
