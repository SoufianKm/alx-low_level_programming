#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * add - adds two integers and returns the result
 *
 * @a, @b: parameters given
 *
 * Return: the sum of parameters
 */
int add(int a, int b)
{
	return (a + b);
}
